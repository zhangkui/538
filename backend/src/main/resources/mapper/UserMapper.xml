<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.mapper.UserMapper">
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO user (username, password, real_name, role, need_change_pwd, class_name)
        VALUES
        <foreach collection="list" item="user" separator=",">
            (#{user.username}, #{user.password}, #{user.realName}, #{user.role}, 
             #{user.needChangePwd}, #{user.className})
        </foreach>
    </insert>
    
</mapper>
